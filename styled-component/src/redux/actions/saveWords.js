import {addActions} from '../types';

const save = words => {
    return {
        type: addActions.SAVE_WORDS,
        payload: 
    }
}

export default function saveWords() {
    return(dispatch, getState) => {
        const words = getState().reducer 
    }
}